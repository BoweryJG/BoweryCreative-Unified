[build]
  base = "payments"
  publish = "dist"
  command = "npm run build"
  # Force build always

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"